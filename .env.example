# Module Federation Remote URLs
NEXT_PUBLIC_REMOTE_AIDATA=http://localhost:3001
NEXT_PUBLIC_REMOTE_GROWTH=http://localhost:3002
NEXT_PUBLIC_REMOTE_IDEAFORGE=http://localhost:3003
NEXT_PUBLIC_REMOTE_DEALFLOW=http://localhost:3004

# ── app-aidata API 鉴权 ──────────────────────────────────────
NEXT_PUBLIC_AIDATA_API_KEY=dev-api-key

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/ai_xyc

# NextAuth
AUTH_SECRET=dev-auth-secret-ai-xyc-2026
AUTH_URL=http://localhost:3100

# ── Anthropic Claude（可选）─────────────────────────────────────
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_MODEL=claude-opus-4-7

# ── LiteLLM Proxy（可选）──────────────────────────────────────────
LITELLM_BASE_URL=
LITELLM_API_KEY=
FALLBACK_MODEL=deepseek-chat

# ── DeepSeek（默认 LLM）────────────────────────────────────────
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# ── LLM 默认 Provider ──────────────────────────────────────
# 切换 provider: glm | deepseek | qwen
# ⚠️ 原默认 glm 走滴滴内网不可达，改为 deepseek 公网
LLM_PROVIDER=deepseek

# GLM（内网不可达，保留配置但改 provider）
GLM_API_KEY=
GLM_BASE_URL=http://llm-proxy.intra.xiaojukeji.com/v1
GLM_MODEL=glm-5-external

# 阿里云通义千问（可选）
QWEN_API_KEY=
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-turbo

# ── DealFlow — 拼多多多多进宝 ───────────────────────────────
PDD_APP_KEY=b50759d6f99d4aa19f9379586d2bdb5c
PDD_APP_SECRET=1827d7b59ab0e77e5791c70ed29b406c4453b5f2
PDD_PID=44471231_316324506

# Worker 同步配置
SYNC_INTERVAL_HOURS=1
SYNC_MAX_PRODUCTS_PER_CATEGORY=50

# API 安全
ALLOWED_ORIGIN=http://localhost:3100
